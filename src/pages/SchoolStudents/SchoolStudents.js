/**
 * Imports
 */

import {component, element} from 'vdux'
import {Block} from 'vdux-ui'

/**
 * <School Students/>
 */

export default component({
  render ({props}) {
    return (
    	<Block>
    		Students dd
    	</Block>
    )
  }
})

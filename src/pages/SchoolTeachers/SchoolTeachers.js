/**
 * Imports
 */

import {component, element} from 'vdux'
import {Block} from 'vdux-ui'

/**
 * <School Teachers/>
 */

export default component({
  render ({props}) {
    return (
    	<Block>
    		Teacher sutgds
    	</Block>
    )
  }
})

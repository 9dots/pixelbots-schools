/**
 * Imports
 */

import {component, element} from 'vdux'
import {Block} from 'vdux-ui'

/**
 * <School Stream/>
 */

export default component({
  render ({props}) {
    return (
    	<Block>
    		Streaming
    	</Block>
    )
  }
})

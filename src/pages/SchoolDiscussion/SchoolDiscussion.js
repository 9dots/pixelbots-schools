/**
 * Imports
 */

import {component, element} from 'vdux'
import {Block} from 'vdux-ui'

/**
 * <School Discussion/>
 */

export default component({
  render ({props}) {
    return (
    	<Block>
    		Discussion School
    	</Block>
    )
  }
})

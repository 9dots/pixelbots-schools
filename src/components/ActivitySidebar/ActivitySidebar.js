/**
 * Imports
 */

import element from 'vdux/element'
import {Card} from 'vdux-ui'

/**
 * <ActivitySidebar/>
 */

function render ({props}) {
  return (
    <Card fixed={{top: 53}} mt>
      test
    </Card>
  )
}

/**
 * Exports
 */

export default {
  render
}
